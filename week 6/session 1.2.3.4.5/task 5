public class Solution {
    public static int marsExploration(String s) {
        String target = "SOS";
        int count = 0;
        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) != target.charAt(i % 3)) count++;
        }
        return count;
    }
}
